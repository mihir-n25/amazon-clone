-> cdn means content delivery network . it catches the content which is closest to end user website run faster due to cdn . it allows for quicker transfer of assests needed for loading content

-> for famous icon like search,home,map we use codes given in font awesome

-> border box means that the border will include in heigth and width itself . if we increase border size heigth and width will be decreased

-> margin sequence - top,right,bottom,left
